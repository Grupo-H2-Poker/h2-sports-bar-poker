/** Espaço padrão acima de cada módulo quando `margin_top` não está no JSON. */
export const MODULO_DEFAULT_MARGIN_TOP: string | number = 40

/** Espaço padrão abaixo de cada módulo quando `margin_bottom` não está no JSON. */
export const MODULO_DEFAULT_MARGIN_BOTTOM: string | number = 0
