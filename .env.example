# API Rewards (login) — server/api/login.post.ts
NUXT_EXTERNAL_API_BASE_RW=

# API cliente / perfil — server/api/users.get.ts
NUXT_EXTERNAL_API_BASE_PLU=

# Conteúdo (unidades / módulos / torneios) no front
# Por padrão usa /api/mock. Só desligue quando a API CMS real aceitar o Worker:
# NUXT_PUBLIC_USE_MOCK_API=false
# NUXT_PUBLIC_EXTERNAL_API_BASE_RW=
